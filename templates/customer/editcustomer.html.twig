{# empty Twig template #}
{% extends "base.html.twig" %}

{% block content %}
    <div class="row">
        <div class="col-lg-2">
        </div>
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header d-flex align-content-center">
                    <h3 class="h4">Modify Existing Customer/Agent</h3>
                </div>
                <div class="card-body">
                    {% for flash_message in app.flashes('modificationsuccess') %}
                        <div class="alert alert-success" role="alert">
                            {{ flash_message }} <br />
                            [<a href="{{path('listcustomer')}}">All Customer</a>] [<a href="{{path('viewcustomer', {'customerid':customerid})}}">View Record</a>]
                        </div>
                    {% endfor %}
                    {{form(form, {'attr': {'novalidate': 'novalidate'}})}}
                </div>
            </div>
        </div>
        <div class="col-lg-2">
        </div>

    </div>
{% endblock content %}
